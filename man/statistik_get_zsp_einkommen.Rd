% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wien_get_income.R
\name{statistik_get_zsp_einkommen}
\alias{statistik_get_zsp_einkommen}
\title{Get Zählsprengel Income Data from Statistik Austria}
\usage{
statistik_get_zsp_einkommen(
  year,
  income_var = c("pers_geseink", "pers_netto", "hh_geseink", "hh_netto"),
  poi = at_income_poi,
  sleep_ms = 10,
  cache_dir = "/Volumes/rr/geodata/österreich/einkommen_zählsprengel",
  verbose = TRUE
)
}
\arguments{
\item{year}{Integer. The year for which to download income data (2012–2022).}

\item{income_var}{Character. Which income variable to download. One of
\code{"pers_geseink"} (default), \code{"pers_netto"}, \code{"hh_geseink"}, \code{"hh_netto"}.}

\item{poi}{An sf object with point geometry (poles of inaccessibility or
centroids) and a \code{ZGEB} column identifying each Zählsprengel. Must be in
EPSG:3857 or any CRS (will be reprojected automatically). Defaults to the
bundled \link{wien_income_poi} dataset covering all Vienna Zählsprengel.
For Austria-wide data use the \code{at_zsp_poi} dataset.}

\item{sleep_ms}{Integer. Delay in milliseconds between requests (default: 100).}

\item{cache_dir}{Character. Path to a directory used to store and resume
intermediate download state. A CSV file named
\verb{zsp_einkommen_<income_var>_<year>.csv} is written after each request.
On subsequent calls with the same arguments, already-queried Zählsprengel
are skipped automatically. Defaults to
\code{"/Volumes/rr/geodata/österreich/einkommen_zählsprengel"}. Set to NULL
to disable caching.}

\item{verbose}{Logical. If TRUE (default), prints progress messages.}
}
\value{
A data frame with columns \code{ID}, \code{g_id}, and the income variable name
(e.g. \code{geseink_mean}), deduplicated by \code{g_id}. Returns NULL if no features
were collected.
}
\description{
Downloads income statistics per Zählsprengel from the Statistik Austria WMS
service. Uses poles of inaccessibility or centroids to query each district
precisely with a single request per Zählsprengel.
}
\details{
Four income variables are available:
\describe{
\item{\code{"pers_geseink"}}{Mean total income incl. transfer payments per person.}
\item{\code{"pers_netto"}}{Mean net income per person.}
\item{\code{"hh_geseink"}}{Mean total income incl. transfer payments per household.}
\item{\code{"hh_netto"}}{Mean net income per household.}
}
}
\examples{
\dontrun{
# Gesamteinkommen per person for 2022 (Vienna, default poi)
statistik_get_zsp_einkommen(2022)

# Nettoeinkommen per person
statistik_get_zsp_einkommen(2022, income_var = "pers_netto")

# Household total income, resumable download cached to disk
statistik_get_zsp_einkommen(2022, income_var = "hh_geseink",
  cache_dir = "data/cache")

# Austria-wide using at_zsp_poi
statistik_get_zsp_einkommen(2022, poi = at_zsp_poi)
}

}
