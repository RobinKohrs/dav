% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dav_use_matilda.R
\name{dav_use_matilda}
\alias{dav_use_matilda}
\title{Use STMatilda Fonts in ggplot2}
\usage{
dav_use_matilda(type = c("text", "info"), base_size = NULL)
}
\arguments{
\item{type}{Character string, either "text" or "info". Determines which font family to use.
Defaults to "text".
\itemize{
\item "text": Uses "STMatilda Text Variable Roman"
\item "info": Uses "STMatilda Info Variable Roman"
}}

\item{base_size}{Optional numeric. If provided, sets the base font size for the plot text.}
}
\value{
A \code{ggplot2::theme} object.
}
\description{
This function returns a ggplot2 theme component that sets the font family
to "STMatilda" (Text or Info variants). It is designed to be added to any
ggplot object or theme.
}
\examples{
\dontrun{
library(ggplot2)

# Use with default theme
ggplot(mtcars, aes(mpg, wt)) +
  geom_point() +
  labs(title = "Matilda Text Font") +
  dav_use_matilda("text")

# Use with theme_kjn or others
ggplot(mtcars, aes(mpg, wt)) +
  geom_point() +
  labs(title = "Matilda Info Font") +
  theme_minimal() +
  dav_use_matilda("info", base_size = 14)
}
}
