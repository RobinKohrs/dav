% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gitlab_fetch_dir.R
\name{gitlab_fetch_dir}
\alias{gitlab_fetch_dir}
\title{Fetch Files from a GitLab Repository Directory}
\usage{
gitlab_fetch_dir(
  gitlab_url,
  project_id,
  repo_path,
  branch,
  download = FALSE,
  output_dir = "gitlab_downloads",
  private_token = NULL
)
}
\arguments{
\item{gitlab_url}{The base URL of the GitLab instance (e.g., "https://gitlab.com").}

\item{project_id}{The ID or URL-encoded path of the project (e.g., "group/project").}

\item{repo_path}{The path inside the repository to list/download files from.}

\item{branch}{The name of the branch.}

\item{download}{Logical. If TRUE, files will be downloaded. Default is FALSE.}

\item{output_dir}{The directory where files should be saved if download is TRUE. Default is "gitlab_downloads".}

\item{private_token}{An optional private token for accessing private repositories.}
}
\value{
A data frame with the list of files (invisibly if downloaded), or NULL if the request fails.
}
\description{
This function lists files in a specific directory of a GitLab repository and optionally downloads them.
}
\examples{
\dontrun{
# List files
files <- gitlab_fetch_dir(
  gitlab_url = "https://gitlab.com",
  project_id = "mygroup/myproject",
  repo_path = "data",
  branch = "main"
)

# Download files
gitlab_fetch_dir(
  gitlab_url = "https://gitlab.com",
  project_id = "mygroup/myproject",
  repo_path = "data",
  branch = "main",
  download = TRUE,
  output_dir = "my_data"
)
}
}
