% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistik_get_1km_pop.R
\name{statistik_get_1km_pop}
\alias{statistik_get_1km_pop}
\title{Get 1km Grid Population Data from Statistik Austria}
\usage{
statistik_get_1km_pop(
  year,
  poi = at_1km_centroids,
  sleep_ms = 10,
  cache_dir = "/Volumes/rr/geodata/österreich/population_1km",
  verbose = TRUE
)
}
\arguments{
\item{year}{Integer. The reference year (e.g. 2025). Passed to the WMS as
\code{YEAR:{year}-01-01}.}

\item{poi}{An sf object with point geometry and a \code{cell_id} column
(standard INSPIRE 1km cell identifier, e.g. \code{"1kmN2390E4636"}). Must be
in EPSG:4326 or any CRS (will be reprojected to EPSG:3857 automatically).
Defaults to the bundled \link{at_1km_centroids} dataset covering all populated
1km cells in Austria.}

\item{sleep_ms}{Integer. Delay in milliseconds between requests (default: 10).}

\item{cache_dir}{Character. Path to a directory used to store and resume
intermediate download state. A CSV file named
\verb{pop_1km_<year>.csv} is written after each request. On subsequent calls
with the same \code{year}, already-queried cells are skipped automatically.
Defaults to \code{"/Volumes/rr/geodata/österreich/population_1km"}. Set to
\code{NULL} to disable caching.}

\item{verbose}{Logical. If \code{TRUE} (default), prints progress messages.}
}
\value{
A data frame with columns \code{cell_id} and \code{hws} (Hauptwohnsitz —
number of persons with main residence in the cell), deduplicated by
\code{cell_id}. Returns \code{NULL} if no features were collected.
}
\description{
Downloads population counts per 1km INSPIRE grid cell from the Statistik
Austria WMS service. Uses pre-computed cell centroids to issue one
GetFeatureInfo request per grid cell.
}
\examples{
\dontrun{
# Austria-wide 1km population grid for 2025
statistik_get_1km_pop(2025)

# Subset to Vienna cells only
wien_cells <- at_1km_centroids[sf::st_within(
  at_1km_centroids,
  sf::st_transform(wien_poly, 4326),
  sparse = FALSE
)[, 1], ]
statistik_get_1km_pop(2025, poi = wien_cells)

# Resumable download with custom cache location
statistik_get_1km_pop(2025, cache_dir = "data/cache")
}

}
