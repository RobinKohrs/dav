% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistik_neuzulassungen_nach_marke.R
\name{statistik_pkw_marken_zeitreihe}
\alias{statistik_pkw_marken_zeitreihe}
\title{Pkw-Neuzulassungen nach Marken ab Jänner 2000}
\source{
\url{https://data.statistik.gv.at/web/meta.jsp?dataset=OGD_fkfzul0759_OD_PkwNZL_1}
}
\usage{
statistik_pkw_marken_zeitreihe(
  date_from = NULL,
  date_to = NULL,
  marken = NULL,
  parse_date = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{date_from}{Date or character \code{"YYYY-MM"}. Keep rows from this month
onward. \code{NULL} (default) returns the full series from January 2000.}

\item{date_to}{Date or character \code{"YYYY-MM"}. Keep rows up to and
including this month. \code{NULL} (default) returns all available data.}

\item{marken}{Character vector of brand names to keep (case-insensitive
substring match against the Austrian brand label, e.g. \code{"VW"},
\code{c("BMW", "MERCEDES")}). \code{NULL} (default) returns all brands.}

\item{parse_date}{Logical. If \code{TRUE} (default), a \code{date} column of class
\code{Date} (first day of each month) is added alongside \code{monat}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{tibble} with columns:
\describe{
\item{\code{monat}}{Austrian month label, e.g. \code{"Jänner 2000"}.}
\item{\code{date}}{\code{Date}: first day of the month (only when \code{parse_date = TRUE}).}
\item{\code{brand}}{Clean brand name without country code or internal code, e.g. \code{"VW"}.}
\item{\code{producing_country}}{ISO vehicle registration country code of the producing country, e.g. \code{"D"} for Germany.}
\item{\code{is_ev_only}}{Logical. \code{TRUE} if the brand sells only battery-electric vehicles (e.g. Tesla, NIO, Polestar).}
\item{\code{marke}}{Full brand label as published by Statistik Austria, e.g. \code{"VW (D) <040540>"}.}
\item{\code{anzahl}}{Integer count of new registrations.}
}
}
\description{
Downloads the official Statistik Austria Open Data dataset
\emph{Pkw-Neuzulassungen nach Marken ab Jänner 2000} (OGD ID:
\code{OGD_fkfzul0759_OD_PkwNZL_1}) via the OGD REST API and returns a tidy
long tibble with one row per brand × month combination.
}
\details{
The dataset covers new passenger car (Pkw) registrations in Austria by
brand and is updated monthly (typically around the 10th of the following
month). Dimension lookup CSVs are downloaded automatically on each call.
}
\examples{
\dontrun{
# Full series, all brands
statistik_neuzulassungen_nach_marke()

# Since 2020, selected brands
statistik_neuzulassungen_nach_marke(
  date_from = "2020-01",
  marken    = c("VW", "SKODA", "BMW", "MERCEDES", "AUDI", "TESLA")
)

# Most recent 3 months, top 10 brands by registrations
df <- statistik_pkw_marken_zeitreihe(date_from = Sys.Date() - 100)
dplyr::slice_max(df, anzahl, by = monat, n = 10)
}
}
