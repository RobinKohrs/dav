% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nice_colors.R
\name{nice_colors}
\alias{nice_colors}
\title{Nice Color Palettes with Contrasting Colors}
\usage{
nice_colors(palette = NULL, include_contrasts = TRUE)
}
\arguments{
\item{palette}{Character. Name of the palette to return. Available palettes:
"modern", "vibrant", "pastel", "professional", "nature", "sunset", "ocean", "newspaper".
If NULL (default), returns all palettes.}

\item{include_contrasts}{Logical. If TRUE (default), includes contrasting colors.
If FALSE, returns only main colors.}
}
\value{
A list containing color palettes. Each palette is a list with:
\itemize{
\item main: Named vector of main colors
\item contrast: Named vector of contrasting colors
\item description: Description of the palette
}
}
\description{
Returns curated color palettes with contrasting colors for each main color.
Each palette includes a main color and its contrasting color for optimal readability.
}
\examples{
# Get all palettes
nice_colors()

# Get specific palette
nice_colors("modern")

# Get only main colors without contrasts
nice_colors("vibrant", include_contrasts = FALSE)

# Use in ggplot
library(ggplot2)
colors <- nice_colors("modern")
ggplot(mtcars, aes(x = wt, y = mpg, color = factor(cyl))) +
  geom_point() +
  scale_color_manual(values = colors$main)

# Use newspaper colors
newspaper_colors <- nice_colors("newspaper")
ggplot(mtcars, aes(x = wt, y = mpg, color = factor(cyl))) +
  geom_point() +
  scale_color_manual(values = newspaper_colors$main)

}
