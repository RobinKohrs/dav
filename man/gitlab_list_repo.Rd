% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gitlab_list_repo.R
\name{gitlab_list_repo}
\alias{gitlab_list_repo}
\title{List Files in a GitLab Repository Recursively}
\usage{
gitlab_list_repo(
  gitlab_url,
  project_id,
  path = "",
  branch = "",
  private_token = NULL
)
}
\arguments{
\item{gitlab_url}{The base URL of the GitLab instance (e.g., "https://gitlab.com").}

\item{project_id}{The ID or URL-encoded path of the project (e.g., "group/project" or "group\%2Fproject").}

\item{path}{The path inside the repository to list files from. Default is the root ("").}

\item{branch}{The name of the branch. Default is the repository's default branch.}

\item{private_token}{An optional private token for accessing private repositories.}
}
\value{
A data frame with the list of files and their attributes, or NULL if the request fails.
}
\description{
This function lists all files in a given path of a GitLab repository recursively.
It uses the GitLab API to fetch the repository tree.
}
\examples{
\dontrun{
# List files in a public repository on gitlab.com
files_df <- gitlab_list_repo(
  gitlab_url = "https://gitlab.com",
  project_id = "gnome/nautilus"
)

if (!is.null(files_df)) {
  print(head(files_df))
}

# List files in a sub-directory of a project
files_in_src <- gitlab_list_repo(
  gitlab_url = "https://gitlab.com",
  project_id = "gnome/nautilus",
  path = "src"
)

if (!is.null(files_in_src)) {
  print(head(files_in_src))
}
}
}
