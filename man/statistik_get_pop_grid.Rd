% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistik_get_pop_grid.R
\name{statistik_get_pop_grid}
\alias{statistik_get_pop_grid}
\title{Download Population Counts for the Statistik Austria INSPIRE Grid}
\usage{
statistik_get_pop_grid(
  year,
  resolution = c("1km", "250m"),
  sleep_ms = 10,
  cache_dir = "/Volumes/rr/geodata/oesterreich/population_grid",
  verbose = TRUE
)
}
\arguments{
\item{year}{Integer. The reference year (e.g. 2025). Passed to the WMS as
\code{YEAR:{year}-01-01}.}

\item{resolution}{Character. Grid resolution: \code{"1km"} (default) or \code{"250m"}.}

\item{sleep_ms}{Integer. Delay in milliseconds between requests (default: 10).}

\item{cache_dir}{Character. Directory used to store the downloaded grid
shapefile and the intermediate WMS query results. A CSV file named
\verb{pop_1km_<year>.csv} or \verb{pop_250m_<year>.csv} is written after each
request, enabling resumable downloads. Defaults to
\code{"/Volumes/rr/geodata/oesterreich/population_grid"}. Set to \code{NULL} to
disable caching (grid and results are kept only in memory / tempdir).}

\item{verbose}{Logical. If \code{TRUE} (default), prints progress messages.}
}
\value{
A data frame with columns \code{cell_id} (INSPIRE grid cell identifier)
and \code{hws} (integer, Hauptwohnsitz — persons with main residence in the
cell). Cells with no registered population are omitted.
}
\description{
Downloads the 1km or 250m INSPIRE LAEA grid shapefile from Statistik Austria,
extracts cell centroids, and queries the WMS \code{GetFeatureInfo} endpoint to
obtain population counts (Hauptwohnsitz) per grid cell.
}
\examples{
\dontrun{
# 1km grid, Austria-wide
pop1km  <- statistik_get_pop_grid(2025)

# 250m grid (much larger — ~1.4 million cells)
pop250m <- statistik_get_pop_grid(2025, resolution = "250m")

# Join back to the grid polygons
grid   <- statistik_get_1km_grid()
result <- dplyr::left_join(grid, pop1km, by = "cell_id")
}

}
