% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wien_get_income.R
\name{wien_get_income}
\alias{wien_get_income}
\title{Get Vienna Zählbezirk Income Data for a Specific Year}
\usage{
wien_get_income(
  year,
  poi = wien_income_poi,
  sleep_ms = 100,
  download_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{year}{Integer. The year for which to download income data (2012-2022).}

\item{poi}{An sf object with point geometry (poles of inaccessibility) and a
\code{ZGEB} column identifying each Zählbezirk. Must be in EPSG:3857 or any
CRS (will be reprojected automatically). Defaults to the bundled
\link{wien_income_poi} dataset covering all Vienna Zählbezirke.}

\item{sleep_ms}{Integer. Delay in milliseconds between requests (default: 100).}

\item{download_dir}{Character. Optional path to a directory where the GeoJSON
file will be saved as \verb{wien_income_<year>.geojson}. If NULL (default), no
file is written to disk.}

\item{verbose}{Logical. If TRUE (default), prints progress messages.}
}
\value{
A data frame with columns \code{ID}, \code{g_id}, and \code{V1} (mean income, numeric),
deduplicated by \code{g_id}. Returns NULL if no features were collected.
}
\description{
Downloads income statistics per Zählbezirk (census district) for Vienna
from the Statistik Austria WMS service. Uses poles of inaccessibility
to query each district precisely with a single request per district.
}
\examples{
\dontrun{
# Get income data for 2022 using the bundled POI data
income_2022 <- wien_get_income(2022)

# Use a custom poi sf object
poi <- sf::st_read("poi.geojson")
income_2022 <- wien_get_income(2022, poi)

# Get income data for 2015 with a slower crawl speed
income_2015 <- wien_get_income(2015, sleep_ms = 300)
}

}
