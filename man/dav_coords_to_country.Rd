% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dav_coords_to_country.R
\name{dav_coords_to_country}
\alias{dav_coords_to_country}
\title{Get Country from Coordinates (Spatial)}
\usage{
dav_coords_to_country(lat, lon)
}
\arguments{
\item{lat}{Numeric vector. Latitude.}

\item{lon}{Numeric vector. Longitude.}
}
\value{
A character vector containing the country names. NA where not found or coordinates are invalid.
}
\description{
This function accepts latitude and longitude coordinates and returns the country name
using a spatial join with data from the \code{rnaturalearth} package.
It is vectorized and efficient for data frames.
}
\examples{
\dontrun{
# Scalar usage
dav_coords_to_country(47.2, 11.4)

# Vectorized usage in dplyr
library(dplyr)
df <- data.frame(lat = c(47.2, 48.8), lon = c(11.4, 2.3))
df \%>\% mutate(country = dav_coords_to_country(lat, lon))
}
}
