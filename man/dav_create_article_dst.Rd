% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dav_create_article_dst.R
\name{dav_create_article_dst}
\alias{dav_create_article_dst}
\title{Create a derstandard.at-styled Quarto Article}
\usage{
dav_create_article_dst(
  ressort,
  article_directory,
  title,
  subtitle,
  author = "",
  overwrite = FALSE
)
}
\arguments{
\item{ressort}{Character string. The derstandard.at ressort (section). Must
be one of \code{"Inland"}, \code{"Ausland"}, \code{"Wirtschaft"}, \code{"Web"}, \code{"Sport"},
\code{"Panorama"}, \code{"Wissenschaft"}, \code{"Technik"}, \code{"Kultur"}, \code{"Etat"},
\code{"Karriere"}, \code{"Immobilien"}, \code{"Genuss"}, \code{"Recht"}, \code{"Diskurs"},
\code{"Album"}, \code{"Dossier"}. Falls back to the Standard red for unknown values.}

\item{article_directory}{Character string. Path to the directory that will
be created. Both the \code{article.qmd} and \code{article-dst.css} files are placed
directly inside this directory.}

\item{title}{Character string. Article headline displayed in the dark header
and used as the HTML page title.}

\item{subtitle}{Character string. Article standfirst / lead text displayed
below the headline in the dark header.}

\item{author}{Character string. Author name shown in the byline.
Defaults to an empty string (byline line is still rendered).}

\item{overwrite}{Logical. If \code{FALSE} (the default), an existing
\code{article.qmd} is left untouched — only \code{article-dst.css} is updated. Set
to \code{TRUE} to also regenerate the QMD (this will discard any content you
have written in the file).}
}
\value{
Invisibly returns the path to the \code{article.qmd} file.
}
\description{
Creates a directory containing a Quarto HTML article (\code{article.qmd}) and a
CSS file (\code{article-dst.css}) that together reproduce the visual identity of
derstandard.at, using the STMatilda font family and ressort-specific accent
colours.
}
\examples{
\dontrun{
dav_create_article_dst(
  ressort          = "Wirtschaft",
  article_directory = "articles/pkw-elektro-2025",
  title            = "Der Elektroauto-Boom stockt",
  subtitle         = "Trotz sinkender Preise sinken die Zulassungszahlen – was steckt dahinter?"
)
}
}
