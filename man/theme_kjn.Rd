% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_kjn.R
\name{theme_kjn}
\alias{theme_kjn}
\title{KJN Theme for ggplot2 with Markdown Support}
\usage{
theme_kjn(
  base_size_px = NULL,
  base_family = "Roboto",
  target_device = "desktop"
)
}
\arguments{
\item{base_size_px}{Base font size in pixels (default 14px for desktop, 12px for mobile).
This will be directly used where \code{element_markdown} supports pixel sizes.
For elements still using \code{element_text} or where \code{rel()} is used with \code{element_markdown},
this acts more like a reference point.}

\item{base_family}{Base font family. Defaults to "Roboto".}

\item{target_device}{A character string: "desktop" (default) or "mobile".
Influences default \code{base_size_px} and relative text size multipliers.}
}
\value{
A \code{ggplot2::theme} object.
}
\description{
This theme provides a clean look for ggplot2 plots, using Roboto and black text,
and leverages \code{ggtext::element_markdown()} for enhanced text styling capabilities
in titles, subtitles, captions, axis titles, and legend text.
It builds upon \code{theme_minimal()}.
}
\details{
This theme attempts to use the "Roboto" font family. For this to work
correctly, \strong{"Roboto" should be installed as a system font}. If not found,
a message is displayed, and \code{ggplot2} may fall back to a default.
Users can also employ the \code{showtext} package for font management.

\strong{Using Markdown:}
When using this theme, you can use markdown/HTML in \code{labs()} for elements like
\code{title}, \code{subtitle}, \code{caption}, \code{x}, \code{y}, and \code{legend.title}.
Example: \code{labs(title = "My <b style='color:blue;'>Awesome</b> Title <small>(in 10px)</small>")}
Note: \code{element_markdown} primarily affects theme elements. For markdown in \code{labs},
\code{ggtext::geom_textbox} or \code{ggtext::geom_richtext} might be needed for complex geoms,
but for \code{labs()} elements, \code{theme_markdown} is often sufficient.
}
\examples{
library(ggplot2)
library(ggtext) # For markdown elements

# --- Optional: Showtext for Roboto if not system-installed ---
#
\dontrun{
#   library(showtext)
#   font_add_google("Roboto", "Roboto")
#   showtext_auto()
#
}

data(mpg, package = "ggplot2")
p_base <- ggplot(mpg, aes(x = displ, y = hwy)) +
  geom_point(aes(color = factor(cyl)), alpha = 0.7)

# --- Desktop Example with Markdown ---
p_desk_md <- p_base +
  theme_kjn_markdown(target_device = "desktop") +
  labs(
    title = "Fuel Efficiency <b style='font-size:22px; color:#0072B2;'>Analysis</b>",
    subtitle = "Investigating the relationship between *engine displacement* and <i>highway MPG</i>.<br>Data from 1999 and 2008.",
    x = "Engine Displacement (<span style='font-style:italic;'>Liters</span>)",
    y = "Highway <span style='font-weight:bold;'>MPG</span>",
    caption = "Source: <span style='color:gray;'>ggplot2 mpg dataset</span> | Chart: KJN",
    color = "<b style='font-size:11px'>Cylinders:</b>"
  )
print(p_desk_md)

# --- Mobile Example with Markdown ---
p_mob_md <- p_base +
  theme_kjn_markdown(target_device = "mobile") +
  labs(
    title = "Fuel Efficiency <b style='font-size:18px; color:#D55E00;'>Analysis</b>",
    subtitle = "Engine displacement vs. <i>highway MPG</i>.<br>Data: 1999 & 2008.",
    x = "Displacement (<span style='font-style:italic;'>L</span>)",
    y = "<span style='font-weight:bold;'>MPG</span> (Highway)",
    caption = "Source: <span style='color:gray;'>mpg data</span> | KJN",
    color = "<b style='font-size:10px'>Cyl.:</b>"
  )
print(p_mob_md)
}
