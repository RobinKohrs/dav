% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html_create_stacked_bar_chart.R
\name{html_create_stacked_bar_chart}
\alias{html_create_stacked_bar_chart}
\title{Create a Responsive Stacked Bar Chart as HTML}
\usage{
html_create_stacked_bar_chart(
  data,
  headline,
  subheader,
  source_text,
  source_link = NULL,
  legend_breakpoint_px = 350,
  border = TRUE,
  show_legend = FALSE
)
}
\arguments{
\item{data}{A list of lists, each with \code{value} (0–100), \code{color} (hex code), optional \code{name}, and optional \code{absolute_value}.}

\item{headline}{A character string for the chart title.}

\item{subheader}{A character string for the chart subtitle.}

\item{source_text}{A character string for the data source.}

\item{source_link}{Optional. A URL to link the source text.}

\item{legend_breakpoint_px}{Integer pixel width below which the legend is shown and labels are hidden.}

\item{border}{Optional. Logical. Whether to show a border around the chart. Defaults to TRUE.}

\item{show_legend}{Optional. Logical. Whether to always show the legend. Defaults to FALSE (responsive behavior).}
}
\value{
A character string containing HTML markup for the chart.
}
\description{
Generates a responsive stacked bar chart HTML string that adjusts its layout based
on screen size. On narrow viewports, inline bar labels are hidden and a legend is shown.
}
\examples{
data = list(
  list(value = 45, color = "#4CAF50", name = "Ja", absolute_value = "45 Personen"),
  list(value = 30, color = "#FF9800", name = "Vielleicht", absolute_value = "30 Personen"),
  list(value = 25, color = "#f44336", name = "Nein", absolute_value = "25 Personen")
)
html = create_responsive_stacked_bar_chart_html(
  data = data,
  headline = "Umfrageergebnis zur neuen Initiative",
  subheader = "Verteilung der Antworten in Prozent",
  source_text = "Interne Studie Q1",
  source_link = "https://example.com/studie",
  border = FALSE
)
if (requireNamespace("htmltools", quietly = TRUE)) {
  htmltools::html_print(htmltools::HTML(html))
}

}
