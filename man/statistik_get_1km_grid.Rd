% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistik_get_1km_grid.R
\name{statistik_get_1km_grid}
\alias{statistik_get_1km_grid}
\title{Download the Statistik Austria 1km LAEA Grid}
\usage{
statistik_get_1km_grid(cache_dir = tempdir(), verbose = TRUE)
}
\arguments{
\item{cache_dir}{Character. Directory where the zip and extracted shapefile
are stored so repeated calls skip the download. Defaults to \code{tempdir()}.
Set to \code{NULL} to always re-download.}

\item{verbose}{Logical. If \code{TRUE} (default), prints progress messages.}
}
\value{
An \code{sf} object with the 1km grid polygons in EPSG:3035 (LAEA Europe).
Contains at minimum a \code{cell_id} column (\code{GRD_ID} renamed) plus any
attributes bundled in the shapefile.
}
\description{
Downloads the official 1km INSPIRE LAEA grid polygon layer from
Statistik Austria's OGD portal, unzips it, reads it as an \code{sf} object,
and optionally joins population data.
}
\examples{
\dontrun{
grid <- statistik_get_1km_grid()

# Join with population data
pop  <- statistik_get_1km_pop(2025)
grid_pop <- dplyr::left_join(grid, pop, by = "cell_id")
}

}
