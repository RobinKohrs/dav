% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html_create_slider.R
\name{html_create_image_slider}
\alias{html_create_image_slider}
\title{Create an HTML Image Comparison Slider}
\usage{
html_create_image_slider(
  image_left_url,
  image_right_url,
  alt_left_image = "Left image",
  alt_right_image = "Right image",
  label_left = NULL,
  label_right = NULL,
  initial_slider_position_percent = 50,
  slide_on_hover = TRUE,
  enable_drag = TRUE,
  container_width = "100\%",
  container_height = "auto",
  handle_line_color = "rgba(255, 255, 255, 0.9)",
  handle_grip_color = "#ffffff",
  handle_arrow_color = "#333333",
  handle_line_width = 2,
  handle_grip_size = 40,
  label_font_family =
    "system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif",
  label_font_size = "0.9em",
  label_text_color = "white",
  label_background_color = "rgba(0, 0, 0, 0.6)",
  label_padding = "5px 10px",
  border_radius = "8px",
  show_labels_on_hover = FALSE,
  slider_aria_label = "Image comparison slider",
  label_position = "bottom"
)
}
\arguments{
\item{image_left_url}{Character string. URL for the "left" or "before" image. (Required)}

\item{image_right_url}{Character string. URL for the "right" or "after" image. (Required)}

\item{alt_left_image}{Character string. Alt text for the left image for accessibility.
Defaults to "Left image".}

\item{alt_right_image}{Character string. Alt text for the right image for accessibility.
Defaults to "Right image".}

\item{label_left}{Optional. Character string. A label displayed on the left image (e.g., "Before").}

\item{label_right}{Optional. Character string. A label displayed on the right image (e.g., "After").}

\item{initial_slider_position_percent}{Numeric (0-100). Initial position of the slider divider.
Defaults to 50 (centered).}

\item{slide_on_hover}{Logical. If TRUE, the slider divider follows the mouse cursor when hovering
over the component. Defaults to TRUE.}

\item{enable_drag}{Logical. If TRUE, the slider can be explicitly dragged with a mouse click or touch.
Defaults to TRUE.}

\item{container_width}{Character string. CSS width for the slider container (e.g., "500px", "100\%").
Defaults to "100\%". Note: The actual width will be constrained to max-width: 615px.}

\item{container_height}{Character string. CSS height for the slider container.
If "auto", height will be determined by the 16:9 aspect ratio.
It's recommended to use images with the same aspect ratio. Defaults to "auto".}

\item{handle_line_color}{Character string. CSS color for the slider's dividing line.
Defaults to "rgba(255, 255, 255, 0.9)".}

\item{handle_grip_color}{Character string. CSS color for the slider's circular drag grip.
Defaults to "#ffffff".}

\item{handle_arrow_color}{Character string. CSS color for the arrows inside the drag grip.
Defaults to "#333333".}

\item{handle_line_width}{Numeric. Width of the dividing line in pixels. Defaults to 2.}

\item{handle_grip_size}{Numeric. Diameter of the circular grip in pixels. Defaults to 40.}

\item{label_font_family}{Character string. CSS \code{font-family} for labels. Defaults to "system-ui, sans-serif".}

\item{label_font_size}{Character string. CSS \code{font-size} for labels. Defaults to "0.9em".}

\item{label_text_color}{Character string. CSS color for label text. Defaults to "white".}

\item{label_background_color}{Character string. CSS \code{background-color} for labels.
Defaults to "rgba(0, 0, 0, 0.6)".}

\item{label_padding}{Character string. CSS padding for labels (e.g., "5px 10px"). Defaults to "5px 10px".}

\item{border_radius}{Character string. CSS \code{border-radius} for the main container. Defaults to "8px".}

\item{show_labels_on_hover}{Logical. If TRUE, labels are only visible when hovering over the slider.
Defaults to FALSE (always visible if provided).}

\item{slider_aria_label}{Character string. ARIA label for the slider functionality, for screen readers.
Defaults to "Image comparison slider".}

\item{label_position}{Character string. Position of the labels: "bottom" (default) or "top".}

\item{#}{--- Accessibility Parameters ---}
}
\description{
Generates HTML, CSS, and JavaScript for a responsive image comparison slider.
Users can slide a divider to reveal more or less of two juxtaposed images.
The slider interacts with mouse hover and touch-drag movements.
All generated CSS classes and the main element ID are prefixed with "dj-"
for better namespacing in CMS environments.
}
