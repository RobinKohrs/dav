% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html_create_number.R
\name{html_create_info_card}
\alias{html_create_info_card}
\title{Create an HTML Info Card}
\usage{
html_create_info_card(
  main_value,
  main_text,
  show_percentage_bar = FALSE,
  percentage_bar_color = "rgba(0, 0, 0, 0.1)",
  ressort_name = NULL,
  background_color = "#f0f0f0",
  text_color = "#333333",
  border_color = "#cccccc",
  headline = NULL,
  source_text = NULL,
  source_link = NULL,
  card_width = "100\%",
  number_format = NULL,
  border_width = 2,
  font_family = "STMatilda Text Variable, system-ui, serif",
  main_value_font_size = "3em",
  main_text_font_size = "1em",
  shadow_intensity = "middle",
  animate_number = FALSE,
  number_animation_duration_ms = 1500,
  number_animation_initial_display = "0",
  sr_only_main_value_label = NULL
)
}
\arguments{
\item{main_value}{Numeric or character. The primary value to display prominently.
If numeric (e.g., 10000.55), it will be formatted according to \code{number_format}.
If character (e.g., "Active"), it will be displayed as-is.
If \code{animate_number} is TRUE and \code{main_value} is numeric, the value will be animated.}

\item{main_text}{Character string. A short descriptive text displayed below the \code{main_value} (e.g., "Users Online", "Completion Rate").}

\item{show_percentage_bar}{Optional. Logical. If TRUE and \code{main_value} is a number between 0 and 100,
displays an animated background bar that fills from left to right as the number animates.}

\item{percentage_bar_color}{Optional. Character string. CSS color for the percentage bar.
Only used if \code{show_percentage_bar} is TRUE. Defaults to "rgba(0, 0, 0, 0.1)".}

\item{ressort_name}{Optional. Character string. The name of a "ressort" (e.g., department, section like 'dst_wirtschaft', 'dst_apo').
If provided and recognized from an internal list, applies a predefined color palette (background, text, border).
Overrides default colors unless specific color arguments are also provided.}

\item{background_color}{Optional. Character string. CSS color for the card's background (e.g., "#f0f0f0", "white").
Defaults to light gray. Overridden by \code{ressort_name} if not explicitly set.}

\item{text_color}{Optional. Character string. CSS color for the card's text (e.g., "#333333", "black").
Defaults to dark gray. Overridden by \code{ressort_name} if not explicitly set.}

\item{border_color}{Optional. Character string. CSS color for the card's border (e.g., "#cccccc", "blue").
Defaults to medium gray. Overridden by \code{ressort_name} (uses accent color) if not explicitly set.}

\item{headline}{Optional. Character string. A headline displayed at the top of the card.}

\item{source_text}{Optional. Character string. Text for a source attribution line (e.g., "Source: Annual Report").}

\item{source_link}{Optional. Character string. A URL to link the \code{source_text} to. \code{source_text} must be provided.}

\item{card_width}{Optional. Character string. CSS width for the card (e.g., "300px", "100\%", "auto").
Defaults to "min(100\%, 350px)" which ensures the card is never wider than 350px but can shrink on smaller screens.}

\item{number_format}{Optional. Character string. The locale to use for number formatting (e.g., "de-DE" for German format).
If NULL, no specific formatting is applied. Common values: "de-DE" (German), "en-US" (US), "fr-FR" (French).
Only applies when \code{animate_number} is TRUE.}

\item{border_width}{Optional. Numeric. Width of the card's border in pixels. Defaults to 2.}

\item{font_family}{Optional. Character string. CSS \code{font-family} string for the card's text.
Defaults to "STMatilda Text Variable, system-ui, serif".}

\item{main_value_font_size}{Optional. Character string. CSS \code{font-size} for the \code{main_value} (e.g., "3em", "48px"). Defaults to "3em".}

\item{main_text_font_size}{Optional. Character string. CSS \code{font-size} for the \code{main_text} (e.g., "1em", "16px"). Defaults to "1em".}

\item{shadow_intensity}{Optional. Character string. Controls the box shadow intensity.
Valid values: "none", "low", "middle" (or "medium"), "intense" (or "high"). Defaults to "middle".}

\item{animate_number}{Optional. Logical. If TRUE and \code{main_value} is parsable as a number,
the \code{main_value} will animate from \code{number_animation_initial_display} to its final state when the card becomes visible.
The animated visual number is \code{aria-hidden="true"} because \code{sr_only_main_value_label} (or fallbacks) provides the complete accessible description.
Defaults to FALSE.}

\item{number_animation_duration_ms}{Optional. Numeric. Duration of the number animation in milliseconds.
Effective only if \code{animate_number} is TRUE. Defaults to 1500.}

\item{number_animation_initial_display}{Optional. Character string. The initial text displayed for \code{main_value} before animation starts
(e.g., "0", "--", "Loading..."). \code{main_value} itself should represent the final target for animation.
Effective only if \code{animate_number} is TRUE. Defaults to "0".}

\item{sr_only_main_value_label}{Optional. Character string. A label to append to \code{main_value} specifically for screen readers.
This helps form a more complete and natural sentence. For example, if \code{main_value} is "10,000" and
\code{sr_only_main_value_label} is "active users", the screen reader would announce "10,000 active users".
If NULL, the function attempts to use \code{main_text} as a fallback label, or just \code{main_value} if \code{main_text} is also empty.
Providing a specific label here is recommended for best accessibility.}
}
\description{
Generates HTML for a responsive info card, suitable for displaying key metrics or pieces of information.
It supports custom styling, ressort-based color palettes, optional animations for numeric values,
and accessibility considerations for screen readers.
}
